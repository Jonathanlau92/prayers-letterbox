<div class="container col-lg-6 offset-lg-3">
  <div class="d-flex justify-content-around">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'form-mobile-width shadow-sm p-5' }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <p class="h4 theme-color text-center mb-4">Forget your password?</p>    

      <div class="form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
      </div>

      <div class="actions mb-4">
        <%= f.submit "Reset Password", class: 'btn btn-primary mt-3' %>
      </div>
      <%= render "devise/shared/links" %>
    <% end %>
  </div>
</div>

